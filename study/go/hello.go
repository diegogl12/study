package main

import "fmt"

func main() {
    var a, pi 
    pi = 3.14159
    fmt.Scanf("%f",&a)
    //c := a + b

    fmt.Printf("A=%.4f",(a*pi*pi))
    // var hello string
    // hello = "hello "
    // world := " go"
    // fmt.Scanf("%s", &world)
	// fmt.Printf(hello+world)
}
